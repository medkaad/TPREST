{% extends 'base.html.twig' %}

{% block title %}Liste des Product{% endblock %}

{% block body %}
    
        <table class="table">
        <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>  
            <th scope="col">Price</th>
        </tr>
        </thead>
        <tbody>
        {% for data in categorie %}
            <tr>
                <td>{{ data.name }}</td>
                <td>{{ data.description }}</td>
                <td>{{ data.price }}</td>
                
            </tr>
            
        {% endfor %}
        </tbody>
        </table>
        <a href="{{ path('product_new') }}"><button>Ajouter Category</button></a>
        
    </div>
{% endblock %}